---
title: "main"
---

<!-- FUNCTIONS -->

``` {r, echo = FALSE, message = FALSE}
R_path <- normalizePath(file.path(dirname(getwd()), "R"))
source(file.path(R_path, "render_helper.R"))
source(file.path(R_path, "render_list_to_rmarkdown.R"))
source(file.path(R_path, "process_yaml.R"))
source(file.path(R_path, "bib.R"))
```

<!-- CHUNK CONFIG -->

```{r, include=FALSE}
knitr::opts_chunk$set(comment = NA)
knitr::opts_chunk$set(dev = c("png")) # important distorted zero in last figure
```

``` {r, echo=F, results="asis", eval = T}
yaml_content <- "../yml/contents.yml"

chapters <- yaml::read_yaml(yaml_content)

process_all_chapters(
  chapters, params_private, params_language, field_names, params_valid_languages,
  params_location, chapters_names, params_profile, params_allow_proof_documents
)
```
